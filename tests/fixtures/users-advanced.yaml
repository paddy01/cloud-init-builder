#cloud-config
users:
  - name: service
    primary_group: service
    shell: /bin/bash
    lock_passwd: true
    homedir: /srv/service
    no_create_home: true
  - name: daemon
    primary_group: daemon
    shell: /usr/sbin/nologin
    lock_passwd: true
    system: true
